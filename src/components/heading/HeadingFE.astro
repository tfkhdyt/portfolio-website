---
import { cn } from '@/lib/utils';

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<h3 class={cn('mb-4 font-mono text-3xl font-extrabold', className)}>
  <span class='text-indigo-500'>{'<'}</span><slot />
  <span class='text-indigo-500'>{' />'}</span>
</h3>
