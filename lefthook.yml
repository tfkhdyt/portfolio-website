pre-commit:
  commands:
    check:
      glob: "./src/**/*.{ts,tsx}"
      run: pnpm biome check --apply-unsafe ./src
pre-push:
  parallel: true
  commands:
    taze:
      run: pnpx taze --failOnOutdated
